import React, { useRef, useEffect } from 'react';
import { Location } from '@reach/router';

import Content from '../content/';

const ContentWrapper = ({ location }) => {
	const contentWrapper = useRef();

	useEffect(() => {});
};
